#!/bin/bash
# Screenshot helper script (Legacy path) — سجّل | Sijil
#
# This file exists to keep legacy tooling/worktree apply steps from failing.
# The active Flutter project lives at the repository root.

echo "Screenshot helper (legacy path)"
echo "For iOS: Use Simulator → File → Save Screen Shot"
echo "For Android: Use Emulator camera icon"

